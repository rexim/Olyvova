package Olyvova::Builder;

use 5.10.1;

use strict;
use warnings;
use utf8;

use Exporter qw(import);

our @EXPORT_OK = qw(single multiple pagination build);

sub single($$$) {
    
}

sub multiple($$) {
    
}

sub pagination($$$) {
    
}

sub build($) {
    
}
